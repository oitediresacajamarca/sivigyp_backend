import { HisHechoMultisectorialEntity } from './his-hecho-multisectorial.entity';

describe('HisHechoMultisectorialEntity', () => {
  it('should be defined', () => {
    expect(new HisHechoMultisectorialEntity()).toBeDefined();
  });
});
