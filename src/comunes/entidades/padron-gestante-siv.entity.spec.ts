import { PadronGestanteSivEntity } from './padron-gestante-siv.entity';

describe('PadronGestanteSivEntity', () => {
  it('should be defined', () => {
    expect(new PadronGestanteSivEntity()).toBeDefined();
  });
});
