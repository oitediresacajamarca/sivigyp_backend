import { DatosComplementarios } from './datos_complementarios';

describe('DatosComplementarios', () => {
  it('should be defined', () => {
    expect(new DatosComplementarios()).toBeDefined();
  });
});
