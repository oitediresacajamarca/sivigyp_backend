import { AtencionRegSemanaEntity } from './atencion-reg-semana.entity';

describe('AtencionRegSemanaEntity', () => {
  it('should be defined', () => {
    expect(new AtencionRegSemanaEntity()).toBeDefined();
  });
});
